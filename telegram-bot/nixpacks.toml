[phases.setup]
aptPkgs = [
    "python3-dev",
    "python3-pip",
    "build-essential",
    "ffmpeg"
]

[phases.install]
cmds = [
    "python -m pip install --upgrade pip setuptools wheel",
    "pip install -r requirements.txt"
]

[start]
cmd = "python main.py"

[build]
cmd = "true"